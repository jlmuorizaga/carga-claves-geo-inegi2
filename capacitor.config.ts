import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'carga-claves-geo-inegi2',
  webDir: 'www'
};

export default config;
